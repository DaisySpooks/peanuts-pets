// Shared starter-pet metadata used by CreatePetScreen (selection UI) and
// App (building the display "species" label for the habitat header).
export const PET_OPTIONS = [
  { key: 'axolotl', label: 'Axolotl' },
  { key: 'betta', label: 'Betta' },
  { key: 'turtle', label: 'Turtle' },
]
